name: my_first_app
description: "A new Flutter project."

publish_to: 'none' 


version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  fluttertoast: ^9.0.0
  url_launcher: ^6.3.2
  image_picker: ^1.2.0
  sqflite: ^2.4.2
  path_provider: ^2.1.5
  http: ^1.2.0
  shared_preferences: ^2.5.3
  firebase_core: ^4.2.0 
  firebase_auth : ^6.1.1
  carousel_slider: ^5.1.1
  cloud_firestore: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # 🎯 CORRECTION 1: flutter_launcher_icons package goes under dev_dependencies
  flutter_launcher_icons: ^0.14.4 
  
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
     - assets/images/
   
    
  #  - assets/fonts/
  fonts:
    - family: PlayFairDisplay
      fonts:
        - asset: assets/fonts/pf_bold.ttf
        - asset: assets/fonts/pf_italic.ttf
        - asset: assets/fonts/pf_medium.ttf
      
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #

# 🎯 CORRECTION 2: The flutter_icons configuration block must be at the root 
# level, outside of the 'flutter:' block. 
flutter_icons:
  android: true
  ios: true
  # Using the general image_path key for the main icon 
  image_path: "assets/icons/app_icon.png" 
  web:
    generate: true
    image_path: "assets/icons/app_icon.png"